
// Aggregate map parts
// Since part0 initialized it as a string (via [] + string coercion) and others appended to it,
// window.japanMapParts is already the full SVG string.
window.japanMapSVG = window.japanMapParts;
